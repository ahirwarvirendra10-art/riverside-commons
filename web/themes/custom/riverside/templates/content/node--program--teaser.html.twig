{{ include('riverside:program-card', {
  title: program_card.title,
  url: program_card.url,
  category: program_card.category,
  description: program_card.description,
  meta: program_card.meta,
  content: content
}) }}